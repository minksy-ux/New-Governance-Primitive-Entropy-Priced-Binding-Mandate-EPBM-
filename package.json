{
  "name": "epbm",
  "version": "0.1.0",
  "description": "Entropy-Priced Binding Mandates — cryptographic governance primitive",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:security": "hardhat test test/EPBM.invariants.test.ts test/EPBM.adversarial.test.ts",
    "coverage": "COVERAGE=true hardhat coverage",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "check:maturity-artifacts": "hardhat run scripts/check-maturity-artifacts.ts",
    "check:formal-evidence": "hardhat run scripts/check-formal-evidence.ts",
    "check:finalization-readiness": "hardhat run scripts/check-finalization-readiness.ts",
    "check:finalization-readiness:local": "hardhat run scripts/check-finalization-readiness.ts --network localhost",
    "export:verifier-history": "hardhat run scripts/export-verifier-governance-history.ts",
    "export:fork-legitimacy": "hardhat run scripts/export-fork-legitimacy-report.ts",
    "simulate:capture": "hardhat run scripts/simulate-capture.ts",
    "simulate:capture:sweep": "hardhat run scripts/sweep-capture.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^20.0.0",
    "dotenv": "^16.4.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.4.0"
  }
}
